# Dependências Opcionais para Cliente Open-Meteo Otimizado

# Para usar o cliente otimizado com cache e retry, instale:
# pip install openmeteo-requests requests-cache retry-requests

# Dependências básicas (já incluídas no projeto):
requests>=2.25.0
pandas>=1.3.0

# Dependências opcionais para funcionalidades avançadas:
openmeteo-requests>=1.0.0     # Cliente oficial otimizado
requests-cache>=1.0.0         # Cache automático de requisições
retry-requests>=2.0.0         # Retry automático em falhas

# Benefícios das dependências opcionais:
# - Performance muito superior (10x-50x mais rápido)
# - Cache automático evita requisições desnecessárias
# - Retry automático para requisições falhadas
# - Processamento direto para NumPy arrays
# - Compatibilidade total com exemplo oficial Open-Meteo
